import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Sign up",
  description: "Sign up",
}

export default function SignUpPage() {
  return (<div>
    <h1>Sign up</h1>
    <form>
    </form>
  </div>)
}